import React, {Component} from 'react';
import './PlayerBar.css';

class PlayerBar extends Component {
        render() {
            return(
                <div id="playerbar">
                    <h1>This will be the music player</h1>
                </div>
            );
        }
}

export default PlayerBar;